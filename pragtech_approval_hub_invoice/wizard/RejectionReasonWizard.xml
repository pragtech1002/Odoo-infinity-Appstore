<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_invoice_rejection_reason_wizard" model="ir.ui.view">
        <field name="name">Rejection Reason Wizard</field>
        <field name="model">invoice.rejection.wizard</field>
        <field name="arch" type="xml">
            <form string="Rejection Reason">
                <group>
                    <field name="invoice_id" invisible="True"/>
                    <field name="reason" widget="text" string="Rejection Reason" required="1"/>
                </group>
                <footer>
                    <button name="action_submit_rejection_reason" string="Submit" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>